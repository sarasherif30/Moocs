<div class="comment-content">
<% @comments.each do |comment| %>

	<p>
		<strong><%= comment.user.try(:name) %></strong><br>
		<%= comment.content %>
		<strong>Posted <%= time_ago_in_words(comment.created_at) %></strong><br>
	</p>

<% end %>
</div>
<%= render 'comments/form' %>

